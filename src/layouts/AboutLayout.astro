---
import type { AboutCommonCopy, AboutPageMeta } from "../content/types";
import {
  ABOUT_LOCALE_META,
  ABOUT_LOCALES,
  type AboutLocale,
  type AboutPageId,
  DEFAULT_LOCALE,
  getAppEntryUrl,
  getCanonicalUrl,
  getPagePath,
} from "../i18n/locales";
import "../styles/site.css";

type Props = {
  locale: AboutLocale;
  page: AboutPageId;
  common: AboutCommonCopy;
  meta: AboutPageMeta;
};

const { locale, page, common, meta } = Astro.props;
const localeMeta = ABOUT_LOCALE_META[locale];
const alternateLinks = ABOUT_LOCALES.map((alternateLocale) => ({
  locale: alternateLocale,
  href: getPagePath(alternateLocale, page),
  canonical: getCanonicalUrl(alternateLocale, page),
  meta: ABOUT_LOCALE_META[alternateLocale],
}));
---

<!doctype html>
<html lang={localeMeta.htmlLang}>
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <meta name="color-scheme" content="light dark" />
    <link rel="icon" type="image/svg+xml" href="/logo.svg" />
    <script is:inline>
      (() => {
        try {
          const storedTheme = localStorage.getItem("rezics-theme");
          document.documentElement.classList.toggle("dark", storedTheme === "dark");
          document.documentElement.classList.toggle("light", storedTheme !== "dark");
        } catch {
          document.documentElement.classList.add("light");
        }
      })();
    </script>
    <title>{meta.title}</title>
    <meta name="description" content={meta.description} />
    {
      alternateLinks.map((link) => (
        <link rel="alternate" hreflang={link.meta.htmlLang} href={link.canonical} />
      ))
    }
    <link
      rel="alternate"
      hreflang="x-default"
      href={getCanonicalUrl(DEFAULT_LOCALE, page)}
    />
    <link rel="canonical" href={getCanonicalUrl(locale, page)} />
    <meta property="og:type" content="website" />
    <meta property="og:site_name" content="Rezics" />
    <meta property="og:title" content={meta.title} />
    <meta property="og:description" content={meta.description} />
    <meta property="og:url" content={getCanonicalUrl(locale, page)} />
  </head>
  <body class="min-h-screen bg-surface-canvas text-text-primary font-sans antialiased">
    <header class="sticky top-0 z-10 border-b border-border-whisper bg-surface-canvas">
      <div class="w-full max-w-7xl mx-auto flex flex-wrap items-center gap-4 px-6 py-4 sm:px-8 lg:flex-nowrap lg:gap-8">
        <a
          class="shrink-0 inline-flex items-center gap-3 text-text-brand no-underline"
          href={getPagePath(locale, "home")}
        >
          <img class="h-7 w-auto" src="/logo.svg" alt="" width="40" height="28" />
          <span class="text-xl font-medium leading-[1.4] tracking-normal">REZICS</span>
        </a>
        <nav class="min-w-0 flex flex-1 items-center gap-2 lg:justify-center" aria-label="Primary navigation">
          <a
            class:list={[
              "rounded-md px-3 py-2 text-sm leading-[1.4] no-underline transition-colors",
              page === "home"
                ? "bg-surface-subtle text-text-brand"
                : "text-text-secondary hover:text-text-primary",
            ]}
            aria-current={page === "home" ? "page" : undefined}
            href={getPagePath(locale, "home")}
          >
            {common.nav.home}
          </a>
          <a
            class:list={[
              "rounded-md px-3 py-2 text-sm leading-[1.4] no-underline transition-colors",
              page === "product"
                ? "bg-surface-subtle text-text-brand"
                : "text-text-secondary hover:text-text-primary",
            ]}
            aria-current={page === "product" ? "page" : undefined}
            href={getPagePath(locale, "product")}
          >
            {common.nav.product}
          </a>
        </nav>
        <div class="flex min-w-0 shrink-0 items-center gap-2">
          <button
            class="inline-flex h-10 shrink-0 items-center justify-center gap-2 rounded-md border border-border-defined px-3 text-sm font-medium leading-[1.4] text-text-primary transition-colors hover:bg-surface-subtle"
            type="button"
            aria-label={common.theme.toggle}
            aria-pressed="false"
            title={common.theme.toggle}
            data-theme-toggle
            data-light-label={common.theme.light}
            data-dark-label={common.theme.dark}
          >
            <svg
              class="h-4 w-4"
              aria-hidden="true"
              data-theme-icon="light"
              viewBox="0 0 24 24"
              fill="none"
              stroke="currentColor"
              stroke-width="2"
              stroke-linecap="round"
              stroke-linejoin="round"
            >
              <circle cx="12" cy="12" r="4"></circle>
              <path d="M12 2v2"></path>
              <path d="M12 20v2"></path>
              <path d="m4.93 4.93 1.41 1.41"></path>
              <path d="m17.66 17.66 1.41 1.41"></path>
              <path d="M2 12h2"></path>
              <path d="M20 12h2"></path>
              <path d="m6.34 17.66-1.41 1.41"></path>
              <path d="m19.07 4.93-1.41 1.41"></path>
            </svg>
            <svg
              class="hidden h-4 w-4"
              aria-hidden="true"
              data-theme-icon="dark"
              viewBox="0 0 24 24"
              fill="none"
              stroke="currentColor"
              stroke-width="2"
              stroke-linecap="round"
              stroke-linejoin="round"
            >
              <path d="M12 3a6.5 6.5 0 0 0 8 8 8 8 0 1 1-8-8Z"></path>
            </svg>
            <span data-theme-label>{common.theme.light}</span>
          </button>
          <a
            class="inline-flex h-10 shrink-0 items-center justify-center rounded-md border border-border-defined px-3 text-sm font-medium leading-[1.4] text-text-primary no-underline transition-colors hover:bg-surface-subtle"
            href={getAppEntryUrl(locale)}
          >
            {common.nav.app}
          </a>
          <details class="language-switcher relative">
            <summary class="flex h-10 cursor-pointer list-none items-center rounded-md px-3 text-sm leading-[1.4] text-text-secondary">
              {common.nav.language}
            </summary>
            <div class="absolute right-0 top-[calc(100%+0.5rem)] z-20 grid min-w-42 gap-1 rounded-md border border-border-whisper bg-surface-base p-2 shadow-modal">
              {
                alternateLinks.map((link) => (
                  <a
                    class:list={[
                      "rounded-sm px-3 py-2 text-sm leading-[1.4] no-underline",
                      link.locale === locale
                        ? "bg-surface-subtle text-text-brand"
                        : "text-text-secondary hover:bg-surface-subtle hover:text-text-primary",
                    ]}
                    aria-current={link.locale === locale ? "true" : undefined}
                    lang={link.meta.htmlLang}
                    href={link.href}
                  >
                    {link.meta.nativeName}
                  </a>
                ))
              }
            </div>
          </details>
        </div>
      </div>
    </header>
    <main class="w-full">
      <slot />
    </main>
    <footer class="w-full border-t border-border-whisper">
      <div class="w-full max-w-6xl mx-auto grid gap-3 px-6 py-8 text-sm leading-[1.55] text-text-tertiary sm:px-8 md:grid-cols-[auto_minmax(0,1fr)] md:justify-between md:gap-8">
        <p class="m-0 text-text-primary">Rezics</p>
        <p class="m-0 md:text-right">{common.footer.originNote}</p>
      </div>
    </footer>
    <script>
      const syncThemeToggle = () => {
        const isDark = document.documentElement.classList.contains("dark");
        document.querySelectorAll<HTMLButtonElement>("[data-theme-toggle]").forEach((button) => {
          const label = button.querySelector<HTMLElement>("[data-theme-label]");
          const lightIcon = button.querySelector<SVGElement>('[data-theme-icon="light"]');
          const darkIcon = button.querySelector<SVGElement>('[data-theme-icon="dark"]');

          button.setAttribute("aria-pressed", String(isDark));
          if (label) {
            label.textContent = isDark ? button.dataset.darkLabel ?? "Dark" : button.dataset.lightLabel ?? "Light";
          }
          lightIcon?.classList.toggle("hidden", isDark);
          darkIcon?.classList.toggle("hidden", !isDark);
        });
      };

      document.querySelectorAll<HTMLButtonElement>("[data-theme-toggle]").forEach((button) => {
        button.addEventListener("click", () => {
          const root = document.documentElement;
          const nextTheme = document.documentElement.classList.contains("dark") ? "light" : "dark";

          root.classList.add("theme-switching");
          root.classList.toggle("dark", nextTheme === "dark");
          root.classList.toggle("light", nextTheme === "light");
          localStorage.setItem("rezics-theme", nextTheme);
          syncThemeToggle();
          requestAnimationFrame(() => {
            root.classList.remove("theme-switching");
          });
        });
      });
      syncThemeToggle();
    </script>
  </body>
</html>
